---
type: action
id: TACTION_FORCE_TRIGGER
title: Force Trigger
summary: Fires every trigger of the named type outright, without examining its events.
valid_values:
  - A trigger type the scenario declares. One trigger is built from it for each tag that carries it, and every one of those fires.
caveats:
  - A trigger already queued for destruction is refused, as a disabled one is.
  - The firing goes to the trigger and not to its tag, so the guard that stops a tag springing while it is already springing does not apply here.
  - The trigger list is walked live. An action inside the forced trigger that removes triggers shortens that list underneath the walk, and an entry that moves down into a position already passed is not reached.
related:
  - type: system
    id: trigger-springing
  - type: action
    id: TACTION_ENABLE_TRIGGER
  - type: action
    id: TACTION_DESTROY_TRIGGER
  - type: action
    id: TACTION_SET_GROUP_ID
---

[Reaching a trigger from another trigger](/systems/trigger-springing/#reaching-a-trigger-from-another-trigger) covers what forcing skips and what it leaves behind.

:::danger[A trigger that forces its own type never returns]
Ordinary springing cannot re-enter itself, because a tag refuses to spring while it is already springing. Forcing bypasses the tag and reaches the trigger directly, so that guard is not in the way. A trigger listing this action against its own type runs its whole action list again from inside itself — including this action — one level deeper each time, with no depth limit and no condition that ends the descent. Two trigger types that force each other do the same.
:::
