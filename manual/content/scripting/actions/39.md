---
type: action
id: TACTION_CHANGE_ZOOM
title: Change Zoom Level...
summary: Runs and does nothing; no zoom level changes.
valid_values:
  - Any number, including the `1` and `2` the editor text names. None of them is read.
caveats:
  - The action runs and does nothing. The number is taken out of the map file and nothing reads it afterward.
  - The radar has a zoomed-out mode, but it is switched by the Radar Toggle control, which itself does nothing in a campaign or standalone mission. This action reaches neither the radar nor the tactical view.
related:
  - type: command
    id: ToggleRadar
  - type: action
    id: TACTION_ZOOM_IN
  - type: action
    id: TACTION_ZOOM_OUT
---
