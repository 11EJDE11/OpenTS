---
type: event
id: TEVENT_PARALYZED
title: Paralyzed
summary: Fires when an electromagnetic pulse or a web reaches the tagged object.
caveats:
  - The event runs no test of its own, so what the two announcements reach is the whole of what it answers to.
  - It is never marked off as having happened, so it is examined afresh on every offer and springs again for each new pulse or web.
  - A web reaches soldiers and nothing else. A vehicle, an aircraft or a structure caught in the same blast is not pinned and springs nothing, and neither does a web immune soldier.
  - A pulse springs the event for several things it does not actually stun — a core defender structure or vehicle, which it leaves running, and a limpet mine, which it destroys outright.
related:
  - type: system
    id: trigger-springing
  - type: system
    id: emp-pulse
  - type: key
    id: Webby
  - type: key
    id: IsWebImmune
  - type: key
    id: Cyborg
---

## The two announcements

[An electromagnetic pulse](/systems/emp-pulse/#what-a-pulse-reaches) springs the event on each object it takes, and that page settles which objects those are. Ordinary infantry are not among them; a [cyborg](/keys/cyborg/) is the only soldier a pulse reaches.

A [`Webby=yes`](/keys/webby/) warhead springs the event on each soldier it pins, and on nothing else it lands on. [`IsWebImmune=yes`](/keys/iswebimmune/) skips the pinning altogether, so an immune soldier takes the hit as ordinary damage and springs nothing.
